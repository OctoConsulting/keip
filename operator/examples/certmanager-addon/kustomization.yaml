resources:
  - testroute.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: testroute-xml
    files:
      - integrationRoute.xml

secretGenerator:
  - name: jks-password
    literals:
      - password=changeit
